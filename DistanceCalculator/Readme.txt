Run DistanceCalculator, and choose the an original image (exmaples in data path).
The the program will run and save stats to ./results, and illustrate the processed images on screen.
If you want to find the stats of some designated cells, you could find their IDs in the ID image and get their stats in the excel file by their IDs.